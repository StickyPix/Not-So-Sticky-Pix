# MCUboot is configured by sysbuild/mcuboot/prj.conf.
#
# Keep this file free of Kconfig assignments so a stale IDE "Extra Kconfig
# fragments" entry that points here cannot apply MCUboot-only symbols to the
# application image.
